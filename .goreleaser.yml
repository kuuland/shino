# .goreleaser.yml
# Build customization
builds:
  - env:
    - GO111MODULE=on
  - binary: shino
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
      - 386
      - arm
# Archive customization
archive:
  format: tar.gz